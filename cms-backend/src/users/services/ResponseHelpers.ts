export interface UserInformation {
  userId: string;
  userName: string;
  photoUrl: string;
  name: string;
  lastName: string;
  role: number[];
  status: number;
  email: string;
  createdAt: string;
}